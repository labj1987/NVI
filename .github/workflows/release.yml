name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install GTK4 and System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-4-dev libadwaita-1-dev pkg-config libssl-dev wget libfuse2t64

      - name: Set up Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Build AppImage
        run: |
          # Inject the official modern Rust path directly into the execution environment
          export PATH="$HOME/.cargo/bin:$PATH"
          bash build-appimage.sh

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: nvidia-driver-installer-*-x86_64.AppImage
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
